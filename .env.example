# Local dev: proxied by vite.config.js to the locally running agent.
# Production build: set to /api/responses instead (same-origin Azure
# Function proxy in front of the Foundry Hosted Agent) via the CI workflow,
# not this file — Vite bakes this value into the static bundle at build time.
VITE_AGENT_URL=/responses
